import { PriorityTransformPipePipe } from './priority-transform.pipe';

describe('PriorityTransformPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityTransformPipePipe();
    expect(pipe).toBeTruthy();
  });
});
