import { StatusTransformPipePipe } from './status-transform.pipe';

describe('StatusTransformPipePipe', () => {
  it('create an instance', () => {
    const pipe = new StatusTransformPipePipe();
    expect(pipe).toBeTruthy();
  });
});
